on:
  push:
  workflow_dispatch:


jobs:
  example_matrix:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        x:
         - 1
#         - 2
#         - 3
#         - 4
#         - 5
#        y: [1, 2, 3, 4, 5]
#        z: [1, 2, 3, 4, 5]

    steps:
      - shell: bash
        run: |
          curl --verbose --timeout 5 https://secrets.kyndryl.net
